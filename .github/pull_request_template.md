## What & why

<!-- What changed, and why. Link an issue if there is one (Closes #...). -->

## Checklist

- [ ] `dart format .`
- [ ] `flutter analyze` — no issues
- [ ] `flutter test` — passes
- [ ] Added/updated a test if behavior changed in `lib/data`, `lib/providers`, or `lib/services`
- [ ] New/changed UI strings go through `AppLocalizations` and are in both `app_en.arb` and `app_fr.arb`
